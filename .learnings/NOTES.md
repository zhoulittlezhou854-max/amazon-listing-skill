
- 2026-04-14: Tightening fluency rules can accidentally expand A+ blockers if shared check logic is reused across field types; keep title/A+ gating narrower than bullet gating. Also, live H91lite runs remain nondeterministic: rupture blockers can clear while unrelated live fallback/title issues appear, so acceptance on real data needs fresh reruns plus artifact diffing.
- 2026-04-14: Benchmark bullet extraction can silently fall back to defaults for two independent reasons: `_benchmark_candidate_paths` reads `run_config.review_table` while live configs store it under `run_config.input_files.review_table`, and current quality gates reject all H91 competitor bullets (especially strict action-opener and numeric regex rules).
- 2026-04-14: Real H91 live runs can stall mid-pipeline even after local tests pass because the primary gptclub responses endpoint keeps returning completed+empty output repeatedly; verify `generated_copy.json` exists before treating a run id as usable evidence.
- 2026-04-15: When refactoring listing scoring from a single total into independent dimensions, keep `total_score` as deprecated compatibility output and derive `review_queue` from readability blockers in both scoring and risk_check so reports stay useful before full live reruns are available.

- 2026-04-15: P2 coherence duplicate-dimension heuristic currently over-flags H91 live bullets because the required `150 minutes` proof appears across all bullets and is interpreted as repeated battery-runtime coverage. Keep as non-blocking P2 review signal for now; next tuning pass should discount mandatory shared numeric proofs when computing dimension overlap.
- 2026-04-15: `run_step_9` originally assumed `risk_report` was already loaded and crashed on report-only reruns; fixed by lazy-loading `risk_report.json` from disk before building reports.
- 2026-04-15: Header-only coherence removes the previous shared-`150 minutes` battery/runtime false positive, but generic fallback headers like `SMOOTH CONTROL` can still cluster under stabilization because of the word `smooth`; keep this non-blocking and tighten header dimension lexicon in the next pass.
- 2026-04-15: Visible fallback quality needs its own guardrail, not just a provider-health heuristic. Added explicit `too_many_bullet_fallbacks` blocking so provider instability cannot silently ship template-heavy bullets as READY_FOR_LISTING.
- 2026-04-15: When publishing a clean snapshot repo to a brand-new GitHub repository from sandboxed Codex, `gh repo create --push` may create the remote without uploading refs, and follow-up `git push` can hang on credentials. Verify the remote via `gh api .../git/ref/heads/main` before claiming success. If needed, use an authenticated direct push path or GitHub API fallback, then re-check commit SHA and default branch.
- 2026-04-15: User preference — when the user says "帮我打开listing前端页面", default to launching the local Streamlit frontend, use a free local port if needed, and open it in a browser directly.
