{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "cd $CLAUDE_PROJECT_DIR && git add . && git commit -m 'auto-checkpoint' --allow-empty 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
